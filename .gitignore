DEBUG
*.fasl
node_modules
roswell/lem-ncurses
*~

contrib/Makefile.am
Makefile.in
compile
configure
config.status
config.log
aclocal.m4
autom4te.cache
install-sh
missing
bin/
system-index.txt

lem-icon.svg

.DS_Store

GIT_IGNORE.*

lem
lem-ncurses
lem-server
lem-client

*.abcl
build/*

*.lx64fsl
.qlot
/systems/

/result
.direnv/

TODO
artifacts/
logs/
.serena/

# macOS deploy artifacts
resources/lem.icns
lem-macos.zip
lem-macos/

# lem-terminal native helper, built from terminal.c via `make terminal-lib`
extensions/terminal/lib/**/*.so

# ACL2 certification build artifacts (SPEC-VK verified/); sources are tracked,
# outputs are not. See scripts/run-proofs.sh.
verified/*.cert
verified/*.cert.out
verified/*.port
verified/*@expansion.lsp
verified/**/*.cert
verified/**/*.cert.out
verified/**/*.port
verified/**/*@expansion.lsp
verified/.sys/

# SPEC-PERF bench outputs: baselines/ and the corpus generators are committed;
# per-run results, profiler dumps, and the generated corpus blobs are not.
# See bench/README.md and scripts/run-bench.sh.
bench/results/
bench/profiles/
bench/corpora/cache/
